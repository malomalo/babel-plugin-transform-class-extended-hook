{
  "name": "babel-plugin-transform-class-extended-hook",
  "version": "1.0.0",
  "description": "Babel plugin that provides a hook for when a class is extended",
  "repository": "malomalo/babel-plugin-transform-class-extended-hook",
  "license": "MIT",
  "main": "dist/index.js",
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "@babel/template": "^7.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/register": "^7.0.0",
    "mocha": "^5.2.0"
  },
  "scripts": {
    "test": "npm run prepublishOnly && npx mocha -r @babel/register 'test/mocha/suite.js'",
    "prepublishOnly": "npx babel src/index.js --out-file dist/index.js"
  },
  "keywords": [
    "babel-plugin",
    "class",
    "inheritance",
    "extended",
    "hook",
    "reflection"
  ]
}
